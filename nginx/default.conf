server {
    listen 80;
    server_name _; # Aceita conexões de qualquer IP/DNS que a VM receber

    # Configuração para passar o tráfego para o Next.js
    location / {
        proxy_pass http://monitor-app:3000; # Usa o nome do serviço definido no compose
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Cabeçalhos extras para repassar o IP real do usuário ao Next.js
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}